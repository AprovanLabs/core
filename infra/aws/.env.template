ENVIRONMENT=prd
PROJECT_ID=aprovan
ORG_ID=aprovan

DOMAIN_NAME=aprovan.com

AWS_DEFAULT_REGION=us-east-2
AWS_ACCOUNT_ID=748469276309
AWS_PROFILE=aprovan

# Comma-separated owner/name repos allowed to assume the CI deploy role
# (CiStack). Defaults to AprovanLabs/registry.
# DEPLOY_REPOSITORIES=AprovanLabs/registry
